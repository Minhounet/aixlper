# Machine-local Claude environment - holds a live credential.
aixlper.env
*.env
!*.env.example

# Generated by scripts/install-global.sh; never a repo artifact.
global/settings.json

__pycache__/
*.pyc

# Eval run output: scores, transcripts and the HTML report. Regenerated by
# `make eval`, and each run's transcripts can be large - never a repo artifact.
skills/*/evals/results/
